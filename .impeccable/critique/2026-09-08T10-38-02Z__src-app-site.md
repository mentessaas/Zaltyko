---
target: src/app/(site)/
mode: Persuade
max_score: 32
total_score: 22
na_heuristics: 7,10
agent_a: aa40a302698b98e8d
agent_b: ae8b5205e7b0a8fc8
p0_count: 1
p1_count: 2
p2_count: 2
detector_findings: 1
timestamp: 2026-09-08T10-38-02Z
slug: src-app-site
---
# Critique: src/app/(site)/ — Zaltyko Landing Site

Method: dual-agent (A: aa40a302698b98e8d · B: ae8b5205e7b0a8fc8)
Mode: Persuade (n/a on heuristics 7 & 10 → /32 max)
Score: 22/32 (68.75%) — Pass

## Heuristic scoring

| # | Heuristic | A | B | Avg |
|---|-----------|---|---|-----|
| 1 | Visibility of system status | 3 | 3 | 3 |
| 2 | Match real world | 3 | 3 | 3 |
| 3 | User control & freedom | 2 | 2 | 2 |
| 4 | Consistency & standards | 2 | 2 | 2 |
| 5 | Error prevention | 2 | 2 | 2 |
| 6 | Recognition over recall | 2 | 2 | 2 |
| 7 | Flexibility & efficiency | n/a | n/a | n/a |
| 8 | Aesthetic & minimalist | 2 | 2 | 2 |
| 9 | Help recover errors | 3 | 2 | 2.5 |
| 10 | Help & documentation | n/a | n/a | n/a |

## Design specificity verdict (A)

The strongest vertical-specificity moment is `src/app/(site)/home/HeroSection.tsx` lines 104-145 — illustrative roster preview with Spanish first names and modality markers. This is exactly the anti-Excel promise: a dueña understands the product without translation.

The weakest moment is the orphan `src/app/(site)/Hero.tsx` with "+120 academias, 18k gimnastas, €3.4M procesado" — a stat claim that PRODUCT.md flags as needing evidence verification. Honest Phase 4 baseline is 2 real academies, 2 live subscriptions, 0/10 pricing interviews.

## Cognitive load failures (4/8 — moderate-high)

- 11+ section stack before footer
- 5× "Crea tu academia gratis" CTA
- 3× "Solicitar demo" CTA
- Decorative search input on /ayuda (renders, does not filter)

## What's working

- Brand voice: colleague-dueña tone, not corporate SaaS
- Font system: Manrope + Space_Grotesk, content-addressed `.p.woff2` preloads for LCP (R6 shipped 2026-09-08)
- Spanish-first: `es_ES` locale everywhere, hreflang ready
- Real product evidence in `/pricing` (no fabricated plans)
- Vertical copy: "clase de hoy", "lo que pasa hoy", gym-specific vocabulary

## Priority issues (synthesis from A + B)

### [P0] Reconcile or delete orphan landing components

- (A) `(site)/Hero.tsx`, `Cta.tsx`, `Faq.tsx`, `Testimonials.tsx`, `MakerIntro.tsx`, `FeaturedTime.tsx`, `pricing.tsx` are NOT imported by `page.tsx` but still ship in the bundle.
- (B) No detector hit on orphans themselves.
- Impact: bundle bloat + drift risk. Two Heros with different headline copy. Orphan `Hero.tsx` uses static `text-5xl`; live `HeroSection.tsx` uses fluid `clamp(...)` — typography drift.
- Suggested command: `/impeccable prune` (delete) or `/impeccable polish` (promote).

### [P1] CTA redundancy

- (A) 5× "Crea tu academia gratis" + 3× "Solicitar demo" on landing.
- (B) No detector hit.
- Impact: CTA fatigue obscures the real entry path (Free self-serve vs Network sales-assisted).
- Suggested command: `/impeccable refine` with content brief to consolidate.

### [P1] Decorative search input on /ayuda

- (A) Search bar renders but does not filter (no client-side results).
- (B) No detector hit.
- Impact: functional deception. WCAG 2.2 AA "Error Prevention" gap.
- Suggested command: `/impeccable refine` — wire `/api/help/search` or remove the input.

### [P2] Replace TestimonialsSection placeholder

- (A) Section uses placeholder copy.
- (B) No detector hit.
- Impact: violates the "Honest scale" product principle (Phase 4 baseline 0/10 interviews).
- Suggested command: `/impeccable refine` — replace with "Sin testimonios fabricados" panel + real 0/10 baseline disclosure.

### [P2] Reconcile GAM/Rítmica scope claim

- (A) Hero says "GAF + GAM + Rítmica", cluster matrix only shows GAF + Rítmica.
- (B) No detector hit.
- Impact: copy inconsistency.
- Suggested command: `/impeccable refine` — align copy with catalog.

## Detector findings (B)

| Detector | Location | Status | Notes |
|---|---|---|---|
| `ai-color-palette` | `src/app/(site)/home/IntegrationsSection.tsx:13` | ⚠️ True positive | `from-violet-600 to-indigo-600` — canonical AI default palette. Replace with brand tokens (`zaltyko-primary`, `zaltyko-accent-teal`). |

Cross-reference observations:

- Hardcoded hex `#00695C` in `ComparisonSection.tsx` — should use brand token
- Mixed shadow systems across `DemoSection`, `StickyCtaBar`, `FinalCtaSection` — consolidate

## Persona red flags (A)

- **Jordan (pragmatist, 35-50, dueña de academia):** "StickyCtaBar blocks content on mobile. Can't read the pricing without dismissing the bar."
- **Riley (researcher, 25-40, evaluating):** "5 CTAs but I don't know which one to pick. The Free vs Starter vs Network distinction isn't clear until /pricing."
- **Casey (skeptic, 30-45):** "The orphan stats on Hero.tsx look made-up. The +120 academies and €3.4M claims have no source — Phase 4 baseline is 0/10."

## Minor observations (A)

- `HeroSection.tsx` uses fluid `clamp(...)`, `Hero.tsx` uses static `text-5xl` — typography drift
- `bottom-nav` shows on public landing for unauthenticated users — should hide on marketing routes
- Spanish vs English locale strings mixed in a few component props
- Missing `aria-current="page"` on the active nav item

## Provocative questions (A)

1. What if "Crea tu academia gratis" is the wrong primary CTA? Data shows 2 real academies came through `Solicitar demo` (sales-assisted), not self-serve.
2. What if the orphan `Hero.tsx` was kept because someone wants to use those stats, but PRODUCT.md's evidence gate forbids it?
3. What if "Rítmica + Artística + Masculina" is the actual addressable market, but the cluster matrix narrows it because SEO is harder for "GAM"?
4. What if the site is doing too much (landing + pricing + contact + ayuda + super-admin + dashboard) for a product with 2 customers?
